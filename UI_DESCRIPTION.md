# 李向前FIRE授权码生成器 - 界面说明

## 程序界面布局

由于运行在无图形环境中，以下是界面的文字描述：

### 界面组成

```
┌─────────────────────────────────────────────────┐
│  【深蓝色标题栏 - 高度80px】                    │
│                                                 │
│      李向前FIRE - 授权码生成器                  │
│       (白色加粗字体，18号)                      │
│                                                 │
│   当前时间: 2025年12月06日 13:05:00             │
│       (灰白色字体，10号)                        │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  主内容区域 (白色背景，有内边距)                │
│                                                 │
│  机器码 (12位):                                 │
│  ┌───────────────────────────────────────────┐  │
│  │   [输入框 - 最多12位，自动大写]           │  │
│  └───────────────────────────────────────────┘  │
│  * 自动转换为大写，最多12位字符                 │
│                                                 │
│  授权码:                                        │
│  ┌───────────────────────────────────────────┐  │
│  │   [只读显示框 - 显示生成的授权码]         │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │生成授权码│ │复制授权码│ │  清空   │           │
│  │ (蓝色) │ │ (绿色) │ │ (红色)  │           │
│  └─────────┘ └─────────┘ └─────────┘           │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 功能说明

1. **标题栏**
   - 深蓝色背景 (#2c3e50)
   - 显示程序名称"李向前FIRE - 授权码生成器"
   - 实时显示当前时间，每秒更新

2. **机器码输入区**
   - 输入框居中对齐
   - 自动将小写字母转换为大写
   - 限制最多输入12位字符
   - 超过12位自动截断
   - 下方有灰色提示文字

3. **授权码显示区**
   - 只读文本框，不可直接编辑
   - 居中对齐显示
   - 白色背景
   - 显示通过计算得出的授权码

4. **操作按钮**
   - **生成授权码** (蓝色按钮): 根据机器码计算并显示授权码
   - **复制授权码** (绿色按钮): 将授权码复制到系统剪贴板
   - **清空** (红色按钮): 清空所有输入和输出

### 交互流程

1. 用户在机器码输入框中输入机器码
2. 输入时自动转换为大写，超过12位自动截断
3. 点击"生成授权码"按钮
4. 程序验证机器码（必须为12位）
5. 计算并显示授权码在下方输出框
6. 用户可点击"复制授权码"按钮复制到剪贴板
7. 点击"清空"可清除所有内容重新开始

### 消息提示

程序会在以下情况显示消息框：
- 未输入机器码时点击"生成授权码"：显示警告
- 机器码不足12位：显示警告
- 生成成功：显示成功提示
- 复制成功：显示成功提示
- 未生成授权码时尝试复制：显示警告

### 窗口属性

- 窗口大小: 500×400 像素
- 窗口不可调整大小
- 窗口标题: "李向前FIRE"
