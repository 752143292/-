# 使用指南 - 李向前FIRE授权码生成器

## 快速开始

### 方式一：直接运行Python程序（推荐用于测试）

**前提条件：**
- 已安装 Python 3.7 或更高版本
- 已安装 tkinter（Python通常自带）

**运行步骤：**

1. 打开命令提示符（Windows）或终端（Linux/Mac）

2. 进入项目目录：
   ```bash
   cd path/to/project
   ```

3. 运行程序：
   ```bash
   python authorization_app.py
   ```
   或在 Linux/Mac 上：
   ```bash
   python3 authorization_app.py
   ```

4. 程序窗口将会打开，您可以开始使用

### 方式二：生成并运行.exe文件（推荐用于分发）

**在Windows上：**

1. 双击运行 `build.bat` 文件
   - 或在命令提示符中运行：`build.bat`

2. 等待构建完成（首次运行会自动安装PyInstaller）

3. 构建完成后，在 `dist` 目录下找到 `李向前FIRE授权码生成器.exe`

4. 双击运行 `李向前FIRE授权码生成器.exe`

**在Linux/Mac上：**

1. 在终端中运行：
   ```bash
   chmod +x build.sh
   ./build.sh
   ```

2. 等待构建完成

3. 运行生成的可执行文件：
   ```bash
   ./dist/李向前FIRE授权码生成器
   ```

## 使用说明

### 界面介绍

程序打开后，您将看到：

1. **顶部标题栏**（深蓝色）
   - 显示程序名称："李向前FIRE - 授权码生成器"
   - 显示当前时间，每秒自动更新

2. **机器码输入区**
   - 标签："机器码 (12位):"
   - 输入框：用于输入机器码
   - 提示文字："* 自动转换为大写，最多12位字符"

3. **授权码显示区**
   - 标签："授权码:"
   - 显示框：显示生成的授权码（只读）

4. **操作按钮**
   - 蓝色按钮："生成授权码"
   - 绿色按钮："复制授权码"
   - 红色按钮："清空"

### 操作流程

#### 1. 输入机器码

- 在"机器码"输入框中输入机器码
- **特性：**
  - 自动将小写字母转换为大写
  - 限制最多12位字符
  - 如果输入超过12位，自动截断

**示例：**
- 输入 `abc123def456` → 自动转换为 `ABC123DEF456`
- 输入 `abc123def456789` → 自动截断为 `ABC123DEF456`

#### 2. 生成授权码

- 点击"生成授权码"按钮
- 程序会验证：
  - 机器码不能为空
  - 机器码必须是12位

**验证成功后：**
- 授权码会显示在下方的"授权码"框中
- 弹出提示："授权码生成成功！"

**验证失败时：**
- 如果未输入机器码：显示警告"请输入机器码！"
- 如果机器码不足12位：显示警告"机器码必须为12位！"

#### 3. 复制授权码

- 点击"复制授权码"按钮
- 授权码会被复制到系统剪贴板
- 弹出提示："授权码已复制到剪贴板！"
- 您可以在任何地方粘贴（Ctrl+V 或 右键→粘贴）

**注意：**
- 如果还没有生成授权码就点击复制，会显示警告："没有可复制的授权码！"

#### 4. 清空重新开始

- 点击"清空"按钮
- 机器码输入框和授权码显示框都会被清空
- 可以重新输入新的机器码

### 完整操作示例

**示例1：正常流程**

1. 输入机器码：`abc123def456`
   - 自动转换显示：`ABC123DEF456`

2. 点击"生成授权码"
   - 显示授权码：`AUTH-ABC123DEF456-DEMO`
   - 提示：生成成功

3. 点击"复制授权码"
   - 提示：已复制到剪贴板
   - 可以在其他地方粘贴使用

4. 点击"清空"开始处理下一个

**示例2：错误处理**

1. 输入机器码：`abc123`（少于12位）

2. 点击"生成授权码"
   - 警告：机器码必须为12位！

3. 补充输入完整的12位机器码后再点击生成

## 常见问题

### Q1: 为什么输入的小写字母自动变成大写？

**A:** 这是程序的设计功能，确保所有机器码统一使用大写格式，避免因大小写不同导致的问题。

### Q2: 我输入了超过12位，为什么只显示前12位？

**A:** 机器码被限制为12位，这是业务规则。超过12位的部分会被自动截断。

### Q3: 授权码是如何计算的？

**A:** 当前版本使用的是**示例算法**（返回格式：`AUTH-机器码-DEMO`）。实际的计算规律需要根据您的业务需求定制。计算逻辑位于代码的 `calculate_auth_code()` 函数中。

### Q4: 如何修改授权码计算规律？

**A:** 
1. 打开 `authorization_app.py` 文件
2. 找到 `calculate_auth_code()` 函数（约在第207行）
3. 在标注为 `TODO` 的位置替换为实际的计算逻辑
4. 保存后重新运行或重新构建exe

### Q5: 生成的.exe可以在没有Python的电脑上运行吗？

**A:** 可以！使用 `build.bat` 或 `build.sh` 生成的.exe文件已经打包了所有必要的依赖，可以在没有安装Python的Windows电脑上直接运行。

### Q6: 时间显示不准确怎么办？

**A:** 程序显示的是系统时间。如果显示不准确，请检查并调整计算机的系统时间设置。

### Q7: 能否在Linux或Mac上运行？

**A:** 可以！
- **直接运行Python程序**：在任何操作系统上都可以运行 `python3 authorization_app.py`
- **打包的可执行文件**：在Linux/Mac上也可以使用 `build.sh` 构建可执行文件

### Q8: 我遇到"没有找到tkinter模块"的错误

**A:** 在某些Linux系统上，需要单独安装tkinter：
```bash
# Ubuntu/Debian
sudo apt-get install python3-tk

# Fedora
sudo dnf install python3-tkinter

# Arch Linux
sudo pacman -S tk
```

## 技术支持

如需定制授权码计算算法或其他功能，请提供：
1. 机器码的格式要求
2. 授权码的计算规律或示例
3. 任何特殊需求

## 文件说明

- `authorization_app.py` - 主程序文件
- `requirements.txt` - Python依赖包列表
- `build.bat` - Windows构建脚本
- `build.sh` - Linux/Mac构建脚本
- `test_authorization.py` - 单元测试文件
- `README.md` - 项目说明文档
- `UI_DESCRIPTION.md` - 界面描述文档
- `USAGE_GUIDE.md` - 本使用指南

## 注意事项

1. **首次构建**：第一次运行构建脚本时会自动下载并安装PyInstaller，可能需要几分钟时间

2. **防病毒软件**：某些防病毒软件可能会将PyInstaller打包的程序标记为可疑文件，这是误报。您可以将其添加到白名单中

3. **授权码算法**：当前使用的是示例算法，请根据实际需求修改 `calculate_auth_code()` 函数

4. **机器码格式**：目前只限制长度为12位，支持字母和数字。如需其他限制（如只允许数字），可以修改验证逻辑

---

**版本：** v1.0  
**最后更新：** 2025年12月06日
