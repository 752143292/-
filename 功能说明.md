# 李向前FIRE 授权码生成器 - 功能说明

## 应用程序界面布局

```
┌─────────────────────────────────────────────────┐
│            李向前FIRE                              │
│       2025-12-06 13:10:25                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  机器码 (12位):                                  │
│  ┌───────────────────────────────────────────┐ │
│  │  [输入框 - 自动转大写，限12位]              │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
│  授权码:                                         │
│  ┌───────────────────────────────────────────┐ │
│  │  [显示框 - 只读]                            │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
│  ┌──────────────┐  ┌──────────────┐           │
│  │ 生成授权码    │  │ 复制授权码    │           │
│  └──────────────┘  └──────────────┘           │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 主要功能详解

### 1. 标题显示
- **位置**: 窗口顶部
- **内容**: "李向前FIRE"
- **字体**: 微软雅黑，18号，加粗
- **功能**: 显示软件名称

### 2. 实时时间显示
- **位置**: 标题下方
- **格式**: yyyy-MM-dd HH:mm:ss (例如: 2025-12-06 13:10:25)
- **更新频率**: 每秒自动更新
- **颜色**: 深蓝色
- **功能**: 显示当前系统时间

### 3. 机器码输入框
- **标签**: "机器码 (12位):"
- **限制**:
  - 最多12个字符
  - 自动将输入转换为大写字母
  - 支持字母和数字
- **字体**: Consolas，12号（等宽字体，便于查看）
- **功能**: 用户输入机器码

### 4. 授权码显示框
- **标签**: "授权码:"
- **特性**:
  - 只读（不可编辑）
  - 灰色背景，表示只读状态
  - 可以选择文本进行复制
- **字体**: Consolas，12号（等宽字体）
- **功能**: 显示计算出的授权码

### 5. 生成授权码按钮
- **文字**: "生成授权码"
- **功能**: 
  - 验证机器码是否输入
  - 验证机器码是否为12位
  - 调用计算方法生成授权码
  - 在授权码框中显示结果
- **提示消息**:
  - 如果未输入机器码：显示"请输入机器码！"
  - 如果机器码不足12位：显示"机器码必须为12位！"

### 6. 复制授权码按钮
- **文字**: "复制授权码"
- **功能**:
  - 检查授权码是否已生成
  - 将授权码复制到系统剪贴板
  - 显示复制成功提示
- **提示消息**:
  - 如果未生成授权码：显示"请先生成授权码！"
  - 复制成功后：显示"授权码已复制到剪贴板！"

## 代码实现特点

### 双语注释
每个类、方法、变量都有中英文双语注释：
```csharp
// UI 组件 / UI Components
private Label lblTitle = null!;  // 标题标签 / Title label
```

### 输入验证
- 机器码长度验证
- 非空验证
- 自动格式化（转大写）

### 用户友好
- 清晰的错误提示
- 成功操作的确认消息
- 直观的界面布局

## 待实现功能

### 授权码计算算法
当前位置：`MainForm.cs` 第184-198行

```csharp
private string CalculateAuthorizationCode(string machineCode)
{
    // TODO: 在这里实现具体的授权码计算逻辑
    // TODO: Implement specific authorization code calculation logic here
    
    // 目前返回一个占位符，等待后续添加实际计算规律
    // Currently returns a placeholder, waiting for actual calculation pattern
    
    // 临时实现：简单的示例计算（这将被实际算法替换）
    // Temporary implementation: simple example calculation
    return "TEMP-" + machineCode.Substring(0, 8);
}
```

**需要做的事情**:
1. 获取实际的授权码计算规律
2. 替换临时实现
3. 确保计算结果符合要求

## 文件结构

```
/
├── LicenseGenerator.csproj    # 项目配置文件
├── Program.cs                 # 程序入口点
├── MainForm.cs                # 主窗体实现（包含所有UI和逻辑）
├── 使用说明.md                 # 使用说明文档
├── .gitignore                 # Git忽略文件配置
└── bin/Release/.../publish/
    └── LicenseGenerator.exe   # 发布的可执行文件
```

## 运行要求

- **操作系统**: Windows 7 或更高版本
- **.NET**: 内置在可执行文件中（自包含部署）
- **依赖项**: 无需额外安装

## 使用流程

1. **启动程序** → 双击 `LicenseGenerator.exe`
2. **输入机器码** → 在第一个文本框输入12位机器码（自动转大写）
3. **生成授权码** → 点击"生成授权码"按钮
4. **查看结果** → 授权码显示在第二个文本框
5. **复制使用** → 点击"复制授权码"按钮，粘贴到需要的地方

## 注意事项

1. 机器码必须是12位，否则无法生成授权码
2. 输入会自动转换为大写，无需手动切换
3. 授权码生成后可以直接复制，无需手动选择
4. 时间显示会自动更新，无需刷新
