# 故障排除指南 / Troubleshooting Guide

## 李向前FIRE - 授权码生成器

如果您遇到"代码打不开，无法运行"的问题，请按以下步骤排查：

---

## 🐍 Python 版本问题排查

### 问题1: 没有安装 Python

**症状 / Symptoms:**
- 双击 `.py` 文件没有反应
- 提示找不到 Python

**解决方案 / Solution:**

**Windows:**
1. 下载 Python: https://www.python.org/downloads/
2. 安装时勾选 "Add Python to PATH"
3. 重启电脑

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install python3 python3-pip python3-tk
```

**macOS:**
```bash
brew install python3
```

---

### 问题2: Python 版本过低

**症状 / Symptoms:**
- 提示语法错误
- 提示不支持的功能

**解决方案 / Solution:**

检查版本:
```bash
python --version
# 或
python3 --version
```

需要 Python 3.7 或更高版本。如果版本过低，请升级。

---

### 问题3: 缺少依赖包

**症状 / Symptoms:**
- 提示 `ModuleNotFoundError: No module named 'pyperclip'`
- 提示 `ModuleNotFoundError: No module named 'tkinter'`

**解决方案 / Solution:**

**安装 pyperclip:**
```bash
pip install pyperclip
# 或
pip3 install pyperclip
```

**安装 tkinter:**
- **Windows/macOS**: tkinter 通常已包含在 Python 中
- **Linux:**
  ```bash
  # Ubuntu/Debian
  sudo apt-get install python3-tk
  
  # CentOS/RHEL
  sudo yum install python3-tkinter
  
  # Fedora
  sudo dnf install python3-tkinter
  ```

---

### 问题4: 权限问题

**症状 / Symptoms:**
- Linux/macOS 上提示 "Permission denied"

**解决方案 / Solution:**

```bash
chmod +x start.sh
./start.sh
```

---

## 🔷 C# 版本问题排查

### 问题1: 没有安装 .NET

**症状 / Symptoms:**
- 无法运行编译命令
- 提示找不到 dotnet

**解决方案 / Solution:**

下载并安装 .NET 8.0 SDK:
https://dotnet.microsoft.com/download/dotnet/8.0

---

### 问题2: 在 Linux 上无法编译

**症状 / Symptoms:**
- 提示 Windows 特定错误

**解决方案 / Solution:**

C# Windows Forms 版本只能在 Windows 上编译和运行。

**建议**: 使用 Python 版本，它支持跨平台。

---

## ✅ 推荐的运行方式

### 最简单的方式 - 使用启动脚本

我们提供了自动化启动脚本，会自动检查和安装依赖：

**Windows:**
```
双击运行: start.bat
```

**Linux/macOS:**
```bash
./start.sh
```

**通用 Python 脚本:**
```bash
python start.py
# 或
python3 start.py
```

---

### 手动运行 Python 版本

1. **安装依赖:**
   ```bash
   pip install -r requirements.txt
   ```

2. **运行程序:**
   ```bash
   python license_generator.py
   ```

---

### 手动运行 C# 版本 (仅 Windows)

1. **直接运行 (开发模式):**
   ```bash
   dotnet run
   ```

2. **编译为 EXE:**
   ```bash
   dotnet publish LicenseGenerator.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o ./output
   ```
   
3. **运行编译后的程序:**
   ```
   双击: output\LicenseGenerator.exe
   ```

---

## 🔍 常见错误信息及解决方案

### 错误: "python is not recognized"

**解决方案:**
- Windows: 重新安装 Python，勾选 "Add Python to PATH"
- 或使用完整路径运行: `C:\Python39\python.exe license_generator.py`

---

### 错误: "ModuleNotFoundError"

**解决方案:**
```bash
pip install -r requirements.txt
```

---

### 错误: "_tkinter.TclError" 或 tkinter 相关错误

**解决方案:**
- **Linux**: 安装 python3-tk
  ```bash
  sudo apt-get install python3-tk
  ```
- **macOS**: 
  ```bash
  brew install python-tk@3.9  # 替换为您的 Python 版本
  ```

---

### 错误: "No clipboard"

**解决方案:**

这是剪贴板功能的问题，不影响核心功能。

**Linux 用户需要安装:**
```bash
sudo apt-get install xclip
# 或
sudo apt-get install xsel
```

---

## 📞 仍然无法运行？

如果您按照以上步骤仍然无法运行，请提供以下信息：

1. **操作系统**: Windows / Linux / macOS (版本号)
2. **Python 版本**: 运行 `python --version` 的输出
3. **错误信息**: 完整的错误提示（截图或文字）
4. **使用的版本**: Python 版本 还是 C# 版本
5. **运行命令**: 您使用的命令

---

## 💡 快速诊断

运行以下命令检查环境：

```bash
# 检查 Python
python --version

# 检查 pip
pip --version

# 检查依赖
pip list | grep pyperclip

# 测试 tkinter
python -c "import tkinter; print('tkinter OK')"

# 测试 pyperclip
python -c "import pyperclip; print('pyperclip OK')"
```

---

## 📝 系统要求

### Python 版本
- **Python**: 3.7 或更高版本
- **操作系统**: Windows 7+, Linux, macOS 10.12+
- **依赖**: pyperclip, tkinter
- **磁盘空间**: < 1 MB

### C# 版本
- **操作系统**: Windows 7 或更高版本
- **.NET SDK**: 8.0 (仅编译时需要)
- **运行时**: 无需 (自包含)
- **磁盘空间**: ~150 MB (编译后)

---

## ✨ 建议

1. **首次使用**: 建议使用启动脚本 (`start.bat` / `start.sh` / `start.py`)
2. **跨平台**: 推荐使用 Python 版本
3. **仅 Windows**: 可以选择 C# 版本，界面更原生

---

**最后更新**: 2025-12-06
